module.exports = [
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/node:crypto [external] (node:crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:crypto", () => require("node:crypto"));

module.exports = mod;
}),
"[externals]/node:events [external] (node:events, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:events", () => require("node:events"));

module.exports = mod;
}),
"[externals]/node:net [external] (node:net, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:net", () => require("node:net"));

module.exports = mod;
}),
"[externals]/node:tls [external] (node:tls, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:tls", () => require("node:tls"));

module.exports = mod;
}),
"[externals]/node:timers/promises [external] (node:timers/promises, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:timers/promises", () => require("node:timers/promises"));

module.exports = mod;
}),
"[externals]/net [external] (net, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("net", () => require("net"));

module.exports = mod;
}),
"[externals]/dns/promises [external] (dns/promises, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("dns/promises", () => require("dns/promises"));

module.exports = mod;
}),
"[externals]/node:assert [external] (node:assert, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:assert", () => require("node:assert"));

module.exports = mod;
}),
"[externals]/node:diagnostics_channel [external] (node:diagnostics_channel, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:diagnostics_channel", () => require("node:diagnostics_channel"));

module.exports = mod;
}),
"[externals]/events [external] (events, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("events", () => require("events"));

module.exports = mod;
}),
"[externals]/node:url [external] (node:url, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:url", () => require("node:url"));

module.exports = mod;
}),
"[externals]/stream [external] (stream, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("stream", () => require("stream"));

module.exports = mod;
}),
"[project]/lib/questions.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "seedQuestions",
    ()=>seedQuestions
]);
const seedQuestions = [
    {
        id: "q1",
        text: "How long have Mom & Dad been together?",
        options: [
            "5 years",
            "6 years",
            "7 years",
            "8 years"
        ],
        correctIndex: 2
    },
    {
        id: "q2",
        text: "What trimester did the parents announce they were pregnant?",
        options: [
            "1st trimester",
            "2nd trimester",
            "3rd trimester",
            "Right at birth"
        ],
        correctIndex: 1
    },
    {
        id: "q3",
        text: "How many pieces of furniture have the parents assembled in the nursery?",
        options: [
            "1",
            "2",
            "3",
            "4"
        ],
        correctIndex: 1
    },
    {
        id: "q4",
        text: "How far along is the mom to be?",
        options: [
            "29 weeks",
            "30 weeks",
            "31 weeks",
            "32 weeks"
        ],
        correctIndex: 2
    },
    {
        id: "q5",
        text: "How many days until the baby's due date?",
        options: [
            "49",
            "54",
            "59",
            "64"
        ],
        correctIndex: 2
    },
    {
        id: "q6",
        text: "How many siblings does the dad to be have?",
        options: [
            "5",
            "6",
            "7",
            "8"
        ],
        correctIndex: 2
    },
    {
        id: "q7",
        text: "How many children do the parents to be want?",
        options: [
            "2",
            "3",
            "4",
            "5"
        ],
        correctIndex: 1
    },
    {
        id: "q8",
        text: "What percentage of babies are born on their exact due date?",
        options: [
            "2%",
            "4%",
            "8%",
            "12%"
        ],
        correctIndex: 1
    },
    {
        id: "q9",
        text: "Which food should babies under 1 year old NOT eat?",
        options: [
            "Honey",
            "Yogurt",
            "Banana",
            "Avocado"
        ],
        correctIndex: 0
    },
    {
        id: "q10",
        text: "What is the most common day of the week for babies to be born in the U.S.?",
        options: [
            "Monday",
            "Tuesday",
            "Friday",
            "Sunday"
        ],
        correctIndex: 1
    },
    {
        id: "q11",
        text: "What is the average length of a full-term pregnancy?",
        options: [
            "36 weeks",
            "38 weeks",
            "40 weeks",
            "42 weeks"
        ],
        correctIndex: 2
    },
    {
        id: "q12",
        text: "How many bones are babies born with?",
        options: [
            "206",
            "230",
            "250",
            "270"
        ],
        correctIndex: 3
    },
    {
        id: "q13",
        text: "How many diapers does a newborn use per day on average?",
        options: [
            "2-4",
            "4-6",
            "6-10",
            "10-14"
        ],
        correctIndex: 2
    },
    {
        id: "q14",
        text: "Which sense is the strongest at birth?",
        options: [
            "Sight",
            "Smell",
            "Taste",
            "Hearing"
        ],
        correctIndex: 1
    },
    {
        id: "q15",
        text: "When do most babies start smiling socially?",
        options: [
            "2 weeks",
            "1 month",
            "2 months",
            "4 months"
        ],
        correctIndex: 2
    }
];
}),
"[project]/lib/redis-game-store.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createGame",
    ()=>createGame,
    "getGameState",
    ()=>getGameState,
    "goToNextQuestion",
    ()=>goToNextQuestion,
    "joinGame",
    ()=>joinGame,
    "startGame",
    ()=>startGame,
    "submitAnswer",
    ()=>submitAnswer
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redis$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/redis/dist/index.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$questions$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/questions.ts [app-route] (ecmascript)");
;
;
const ROOM_TTL_MS = 1000 * 60 * 60 * 6;
const QUESTION_DURATION_MS = 20_000;
const REVEAL_DURATION_MS = 3_500;
const LEADERBOARD_DURATION_MS = 5_000;
const MAX_CORRECT_POINTS = 1000;
const MIN_CORRECT_POINTS = 250;
const CODE_CHARS = "ABCDEFGHJKMNPQRSTUVWXYZ23456789";
const AVATARS = [
    "👶",
    "🍼",
    "🧸",
    "🛻",
    "⚽",
    "🦕",
    "🎈",
    "🎵",
    "💙"
];
const redis = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$redis$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createClient"])({
    url: process.env.REDIS_URL || process.env.KV_REST_API_URL,
    password: process.env.REDIS_PASSWORD || process.env.KV_REST_API_TOKEN
});
// Connect to Redis
if (!redis.isOpen) {
    redis.connect().catch(console.error);
}
redis.on('error', (err)=>console.error('Redis Client Error', err));
function now() {
    return Date.now();
}
function randomCode() {
    let code = "";
    for(let i = 0; i < 5; i += 1){
        code += CODE_CHARS[Math.floor(Math.random() * CODE_CHARS.length)];
    }
    return code;
}
async function createCode() {
    const cutoff = now() - ROOM_TTL_MS;
    // Clean up expired rooms
    const keys = await redis.keys('game:*');
    for (const key of keys){
        const roomData = await redis.get(key);
        if (roomData) {
            const room = JSON.parse(roomData);
            if (room.updatedAt < cutoff) {
                await redis.del(key);
            }
        }
    }
    // Generate unique code
    let code = randomCode();
    let exists = await redis.exists(`game:${code}`);
    while(exists){
        code = randomCode();
        exists = await redis.exists(`game:${code}`);
    }
    return code;
}
function sortPlayers(players) {
    return [
        ...players
    ].sort((a, b)=>{
        if (b.score !== a.score) {
            return b.score - a.score;
        }
        return a.joinedAt - b.joinedAt;
    });
}
function tickRoom(room) {
    const currentTime = now();
    if (room.phase === "question_active" && room.questionStartedAt) {
        const elapsed = currentTime - room.questionStartedAt;
        const everyoneAnswered = room.players.length > 0 && room.players.every((player)=>room.currentAnswers[player.id] !== undefined);
        if (elapsed >= QUESTION_DURATION_MS || everyoneAnswered) {
            room.phase = "revealing";
            room.revealStartedAt = currentTime;
            room.leaderboardStartedAt = null;
            room.updatedAt = currentTime;
        }
    }
    if (room.phase === "revealing" && room.revealStartedAt) {
        if (currentTime - room.revealStartedAt >= REVEAL_DURATION_MS) {
            if (room.currentQuestionIndex >= room.questions.length - 1) {
                room.phase = "finished";
            } else {
                room.phase = "leaderboard";
                room.leaderboardStartedAt = currentTime;
            }
            room.updatedAt = currentTime;
        }
    }
    if (room.phase === "leaderboard" && room.leaderboardStartedAt) {
        if (currentTime - room.leaderboardStartedAt >= LEADERBOARD_DURATION_MS) {
            if (room.currentQuestionIndex >= room.questions.length - 1) {
                room.phase = "finished";
            } else {
                room.currentQuestionIndex += 1;
                room.phase = "question_active";
                room.currentAnswers = {};
                room.questionStartedAt = currentTime;
                room.revealStartedAt = null;
                room.leaderboardStartedAt = null;
            }
            room.updatedAt = currentTime;
        }
    }
}
async function getRoomOrThrow(code) {
    const roomData = await redis.get(`game:${code.toUpperCase()}`);
    if (!roomData) {
        throw new Error("Game room not found");
    }
    const room = JSON.parse(roomData);
    // Check if room is expired
    if (now() - room.updatedAt > ROOM_TTL_MS) {
        await redis.del(`game:${code}`);
        throw new Error("Game room not found");
    }
    tickRoom(room);
    await redis.set(`game:${code}`, JSON.stringify(room), {
        EX: Math.floor(ROOM_TTL_MS / 1000)
    });
    return room;
}
function buildPublicState(room, playerId, hostKey) {
    const leaderboard = sortPlayers(room.players);
    const currentQuestion = room.questions[room.currentQuestionIndex] ?? null;
    const yourAnswer = playerId ? room.currentAnswers[playerId] : undefined;
    const isHostView = !!hostKey && hostKey === room.hostKey;
    const timeRemainingMs = room.phase === "question_active" && room.questionStartedAt ? Math.max(0, QUESTION_DURATION_MS - (now() - room.questionStartedAt)) : room.phase === "leaderboard" && room.leaderboardStartedAt ? Math.max(0, LEADERBOARD_DURATION_MS - (now() - room.leaderboardStartedAt)) : 0;
    const reveal = (room.phase === "revealing" || room.phase === "leaderboard" || room.phase === "finished") && currentQuestion ? {
        correctIndex: currentQuestion.correctIndex,
        correctOption: currentQuestion.options[currentQuestion.correctIndex],
        correctPlayerIds: Object.values(room.currentAnswers).filter((answer)=>answer.isCorrect).map((answer)=>answer.playerId)
    } : null;
    return {
        code: room.code,
        phase: room.phase,
        isHostView,
        players: room.players,
        leaderboard,
        totalQuestions: room.questions.length,
        currentQuestionIndex: room.currentQuestionIndex,
        timeRemainingMs,
        question: currentQuestion && room.phase !== "waiting" ? {
            text: currentQuestion.text,
            options: currentQuestion.options
        } : null,
        hasAnswered: !!yourAnswer,
        yourAnswerIndex: yourAnswer?.optionIndex ?? null,
        yourAnswerCorrect: yourAnswer?.isCorrect ?? false,
        reveal
    };
}
async function createGame() {
    const code = await createCode();
    const timestamp = now();
    const hostKey = crypto.randomUUID();
    const room = {
        code,
        hostKey,
        phase: "waiting",
        players: [],
        questions: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$questions$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["seedQuestions"],
        currentQuestionIndex: 0,
        currentAnswers: {},
        questionStartedAt: null,
        revealStartedAt: null,
        leaderboardStartedAt: null,
        createdAt: timestamp,
        updatedAt: timestamp
    };
    await redis.set(`game:${code}`, JSON.stringify(room), {
        EX: Math.floor(ROOM_TTL_MS / 1000)
    });
    return {
        code,
        hostKey
    };
}
async function joinGame(code, name, avatar) {
    const room = await getRoomOrThrow(code);
    if (room.phase !== "waiting") {
        throw new Error("Game has already started");
    }
    const cleanName = name.trim();
    if (!cleanName) {
        throw new Error("Player name is required");
    }
    const playerId = crypto.randomUUID();
    const player = {
        id: playerId,
        name: cleanName.slice(0, 24),
        avatar: avatar && avatar.trim() ? avatar.trim() : AVATARS[Math.floor(Math.random() * AVATARS.length)],
        score: 0,
        joinedAt: now()
    };
    room.players.push(player);
    room.updatedAt = now();
    await redis.set(`game:${code}`, JSON.stringify(room), {
        EX: Math.floor(ROOM_TTL_MS / 1000)
    });
    return {
        player,
        state: buildPublicState(room, playerId)
    };
}
async function getGameState(code, playerId, hostKey) {
    const room = await getRoomOrThrow(code);
    return buildPublicState(room, playerId, hostKey);
}
async function startGame(code, hostKey) {
    const room = await getRoomOrThrow(code);
    if (room.hostKey !== hostKey) {
        throw new Error("Only the host screen can start the game");
    }
    if (room.players.length < 1) {
        throw new Error("At least one player is required");
    }
    if (room.phase !== "waiting") {
        throw new Error("Game has already started");
    }
    room.phase = "question_active";
    room.currentQuestionIndex = 0;
    room.currentAnswers = {};
    room.questionStartedAt = now();
    room.revealStartedAt = null;
    room.leaderboardStartedAt = null;
    room.updatedAt = now();
    await redis.set(`game:${code}`, JSON.stringify(room), {
        EX: Math.floor(ROOM_TTL_MS / 1000)
    });
    return buildPublicState(room, null, hostKey);
}
function getPlayer(room, playerId) {
    const player = room.players.find((entry)=>entry.id === playerId);
    if (!player) {
        throw new Error("Player not found in this room");
    }
    return player;
}
async function submitAnswer(code, playerId, optionIndex) {
    const room = await getRoomOrThrow(code);
    const player = getPlayer(room, playerId);
    if (room.phase !== "question_active" || room.questionStartedAt === null) {
        throw new Error("Question is not active right now");
    }
    const question = room.questions[room.currentQuestionIndex];
    if (!question) {
        throw new Error("Question not found");
    }
    if (optionIndex < 0 || optionIndex >= question.options.length) {
        throw new Error("Invalid answer option");
    }
    if (room.currentAnswers[playerId]) {
        return buildPublicState(room, playerId);
    }
    const submittedAt = now();
    const responseMs = submittedAt - room.questionStartedAt;
    const isCorrect = question.correctIndex === optionIndex;
    const speedFactor = Math.max(0, Math.min(1, (QUESTION_DURATION_MS - responseMs) / QUESTION_DURATION_MS));
    const pointsRange = MAX_CORRECT_POINTS - MIN_CORRECT_POINTS;
    const pointsAwarded = isCorrect ? MIN_CORRECT_POINTS + Math.round(pointsRange * speedFactor) : 0;
    const answer = {
        playerId,
        questionIndex: room.currentQuestionIndex,
        optionIndex,
        submittedAt,
        responseMs,
        isCorrect,
        pointsAwarded
    };
    room.currentAnswers[playerId] = answer;
    player.score += pointsAwarded;
    room.updatedAt = now();
    tickRoom(room);
    await redis.set(`game:${code}`, JSON.stringify(room), {
        EX: Math.floor(ROOM_TTL_MS / 1000)
    });
    return buildPublicState(room, playerId);
}
async function goToNextQuestion(code, hostKey) {
    const room = await getRoomOrThrow(code);
    if (room.hostKey !== hostKey) {
        throw new Error("Only the host can advance the game");
    }
    if (room.phase !== "leaderboard") {
        throw new Error("Game is not ready for next question");
    }
    if (room.currentQuestionIndex >= room.questions.length - 1) {
        room.phase = "finished";
        room.updatedAt = now();
        await redis.set(`game:${code}`, JSON.stringify(room), {
            EX: Math.floor(ROOM_TTL_MS / 1000)
        });
        return buildPublicState(room, null, hostKey);
    }
    room.currentQuestionIndex += 1;
    room.phase = "question_active";
    room.currentAnswers = {};
    room.questionStartedAt = now();
    room.revealStartedAt = null;
    room.leaderboardStartedAt = null;
    room.updatedAt = now();
    await redis.set(`game:${code}`, JSON.stringify(room), {
        EX: Math.floor(ROOM_TTL_MS / 1000)
    });
    return buildPublicState(room, null, hostKey);
}
}),
"[project]/app/api/game-state/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "GET",
    ()=>GET
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/server.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$redis$2d$game$2d$store$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/redis-game-store.ts [app-route] (ecmascript)");
;
;
async function GET(request) {
    try {
        const { searchParams } = new URL(request.url);
        const code = searchParams.get("code");
        const playerId = searchParams.get("playerId");
        const hostKey = searchParams.get("hostKey");
        if (!code) {
            return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
                error: "Room code is required"
            }, {
                status: 400
            });
        }
        const state = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$redis$2d$game$2d$store$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getGameState"])(code, playerId, hostKey);
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            state
        });
    } catch (error) {
        const message = error instanceof Error ? error.message : "Could not load game state";
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: message
        }, {
            status: 404
        });
    }
}
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__0680fcfe._.js.map